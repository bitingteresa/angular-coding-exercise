gs-status-bar(ng-model='dm.state.connection')
div.container
  div.row
    div.col-sm-4
      div.welcome
        h1.welcome__title
          span(ng-if='dm.state.connection.error') Something went wrong while bootstrapping the {{ dm.title }}
          span(ng-if='dm.state.connection.success') Time to get crackin' on {{ dm.title }}
    div.col-sm-4
      p once again try it
      a(ui-sref='test')  test  
    div.col-sm-4 	  
      okay so focus {{dm.excellent[1]}} 
